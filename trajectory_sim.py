# v_0 = initial velocity. Approx. 75m/s
# g = -9.8 m/s^2
# distance (x) at a given time => x(t) = (v_0 * cosM)t
# m = launch angle. Approx. 11deg
# height (y) at a given time => y(t) = (v_0 * sinM)- (gtA^2)/2

# Accounting for the Magnus Effect
# d = density of air at 60Â° = 2.37 * 10-Â³
# v = velocity
# r = ball radius
# av = angular velocity in radians
# n = spin in rpms
# av = (2pi * n) / 60
# FL = (dvr4 * av * 2diA^2)(2r)
# FL = (dvr4 * av * 2diA^2)(2r)
# Drag Force
# F_d = -C_d * v
# F_d = (-C_d * V_x)(-C_d * V_y)(-C_d * v_z)

# Terminal velocity = 32m/s
